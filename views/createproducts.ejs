<%- include('./partials/header') %>

<% if (success && success.length > 0) { %>
    <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
        <span class="inline-block mt-1 mb-1 text-white">
            <%= success.join(', ') %> <!-- Join array into a string for display -->
        </span>
    </div>
<% } %>

<div class="min-h-screen flex flex-col">
    <div class="container px-10 py-20 flex flex-grow gap-20">
        <div class="w-[18%] flex h-40 flex-col items-start bg-slate-200 text-white p-4">
            <div class="flex flex-col text-2xl text-black">
                <a class="block w-fit mb-2 hover:text-blue-300" href="">All Products</a>
                <hr class="border-t border-gray-900 my-2">
                <a class="block w-fit mb-2 hover:text-blue-300" href="">Create new product</a>
            </div>
        </div>
        
        <main class="w-3/4 bg-gray-800 text-white p-8 shadow ml-4">
            <h2 class="text-3xl text-center font-bold mb-4">Create New Product</h2>
            <hr>
            <form autocomplete="off" action="/products/create" method="post" enctype="multipart/form-data">
                <div class="flex gap-10">
                    <div class="flex-1">
                        <h3 class="text-2xl mt-5 font-semibold mb-2">Product Details</h3>
                        <div class="mb-4">
                            <label class="block mb-6 font-bold">Upload Product Image</label>
                            <input name="image" type="file" value="Select File">
                        </div>
                        <div class="grid gap-4">
                            <input name="name" type="text" placeholder="Product Name" class="border p-2 rounded w-full">
                            <input name="price" type="text" placeholder="Product Price" class="border p-2 rounded w-full">
                            <input name="discount" type="text" placeholder="Discount Price" class="border p-2 rounded w-full">
                        </div>
                    </div>

                    <div class="flex-1">
                        <h3 class="text-2xl mt-28 font-bold mb-2">Panel Details</h3>
                        <div class="grid  gap-4">
                            <input name="bgcolor" type="text" placeholder="Background Color" class="border p-2 rounded w-full">
                            <input name="panelcolor" type="text" placeholder="Panel Color" class="border p-2 rounded w-full">
                            <input name="textcolor" type="text" placeholder="Text Color" class="border p-2 rounded w-full">
                        </div>
                    </div>
                </div>
                <div class="flex justify-center mt-12">
                    <input class="px-5 py-2 rounded bg-blue-500 text-white" type="submit" value="Create New Product">
                </div>
            </form>
        </main>
    </div>
</div>

<%- include('./partials/footer') %>
